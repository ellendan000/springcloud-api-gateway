eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8181/eureka
    fetch-registry: true
    register-with-eureka: true

endpoints:
  sensitive: false
management:
  security:
    enabled: false

